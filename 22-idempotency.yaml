- name: create user # number of time you run the script , you get the same result
  hosts: localhost
  connection: localhost
  become: yes
  tasks:
    - name: create expense user
      user: 
        name: expense