- name: demo on loops
  hosts: localhost
  connections: localhost
  become: yes
  tasks:
    - name: print names
      debug:
        msg: " Hi, {{item}}" # item is called reserved spl variable
      loop:
       - ramesh
       - mahesh
       - ravi
