- name: demo on loops
  hosts: localhost
  connection: local
  tasks:
    - name: Print items in a loop
      ansible.builtin.debug:
        msg: "{{ item }}"
      loop:
        - one
        - two
        - three
