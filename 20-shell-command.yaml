- name: demo on shell vs command
  hosts: localhost
  connection: localhost
  tasks:
    - name: command test
      shell: cat /etc/passwd | grep root
      register: user_output

    - name: print output
       debug:
         msg: "{{user_output}}"



