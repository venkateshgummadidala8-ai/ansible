- name: demo on preference vars
  hosts: frontend
  vars:
     full_name: " siva from paly "
  vars_files:
      - course.yaml
  vars_promt:
     - name: full_name
       promt: "Please enter your full_name"
       private: false
  tasks:
     full_name: " siva from task level "
  debug:
     msg: " hi, {{ full_name}}"